#include <stdio.h>
#include <stdio.h>

/**
 * main - Entry point prints alphabet in lowercase\n
 * Return: 0 when success
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
